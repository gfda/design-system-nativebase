import colors from '../base/colors';

const baseStyle = {
  _light: {
    bg: colors.customLight['--p-surface'],
  },
  _dark: {
    bg: colors['--p-interactive-critical'],
  },
};

export default {
  baseStyle,
};

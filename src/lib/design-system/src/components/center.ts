import colors from '../base/colors';

const baseStyle = {
  _light: {
    bg: colors.customLight['--p-surface'],
  },
  _dark: {
    bg: colors.customDark['--p-surface-dark'],
  },
};

export default {
  baseStyle,
};
